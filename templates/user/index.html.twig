{% extends 'base.html.twig' %}

{% block title %}Hello UserController!{% endblock %}

{% block body %}
    <div class="container">
        <div class="text-center">
            {% for user in users %}
                <h3>{{ users|length }}</h3>
            {% endfor %} 
        </div>
    </div>
{% endblock %}
